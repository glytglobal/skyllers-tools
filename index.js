const { chatgpt, mylogic, takanashi, github, vault, yotools } = require("./soft");

module.exports = {
  ai: {
    chatgpt,
    mylogic,
    takanashi
  },
  uploader: {
    github,
    vault,
    yotools
  },
};
